# instructions.md

## Current Plan

1. Add the diagram PNG to `docs/`.
2. Update `README.md` to show the diagram and explain the loop.
3. Create a sample task folder with `TASK.md`, `instructions.md`, and `status.md`.
4. Review the repo for naming consistency and clarity.
5. Commit the changes once the sample is coherent.

## Worker Notes

- Treat `TASK.md` as the source of objective truth.
- Use `status.md` to record what actually happened.
- If review rejects the result, revise this file and run the loop again.
